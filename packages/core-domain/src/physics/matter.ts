import Matter from 'matter-js';

export const { Bodies, Body, Composite, Engine, Events, World, Vector } = Matter;

export type {
    Body as MatterBody,
    Composite as MatterComposite,
    Engine as MatterEngine,
    Events as MatterEvents,
    IEventCollision,
    World as MatterWorld,
    Vector as MatterVector,
} from 'matter-js';
